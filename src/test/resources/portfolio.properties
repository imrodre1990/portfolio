#Modify only if tests are executed against a different browser. Browser capaibilities file must exist with basic configuration.
capabilities=/chrome.capabilities
#  Valid values are = [PROD, UAT, SIT, DEV, QA]
environment=DEV
# Do not modify properties below.
site.url=https://{ENV}.closing.com/{PATH}
api.url=https://ccfees-{RESOURCE}.cntvusw1{ENV}.solutions.corelogic.com/api
grid.url=
#   CHROME, PHANTOMJS
browser=CHROME 